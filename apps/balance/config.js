const path = require('path');

module.exports = {
  name: `Balance`, // replace with your applications human readable name
  path: path.basename(__dirname),
  controller: 'controller.js',
  symbol: 'fas fa-money-bill' // use classes for Fontawesome fonts here ( https://fontawesome.com/icons?d=gallery&m=free )
};
